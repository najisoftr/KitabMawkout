@inherits LayoutComponentBase
@inject NavigationManager Nav
<RadzenComponents/>
@* <div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4">
            <a href="https://learn.microsoft.com/aspnet/core/" target="_blank">About</a>
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div> *@

<RadzenLayout dir="rtl">
    <RadzenBody class="bg-dark bg-gradient">
            @Body
    </RadzenBody>

    <RadzenFooter class="bg-dark bg-opacity-75">
    <div class="row">
        <div class="col-4">
                <button class="btn btn-outline-warning w-100" @onclick="@(()=>{Nav.NavigateTo("/");})">
                    <img src="images/main-menu.png" style="width:36px;"/>
                    <span class="text-light" style="font-size: 20px;">الصفحة الرئيسية</span>
                </button>
        </div>

        <div class="col-4">
                <button class="btn btn-outline-warning w-100" @onclick="@(()=>{Nav.NavigateTo("/settings");})">
                    <img src="images/settings.png" style="width:36px;"/>
                    <span class="text-light fw-bolder" style="font-size: 18px;">الإعدادات</span>
                </button>
        </div>

            <div class="col-4">
                <button class="btn btn-outline-warning w-100" @onclick="@(() => { Nav.NavigateTo("/"); })">
                    <img src="images/informative.png" style="width:36px;" />
                    <span class="text-light fw-bolder" style="font-size: 18px;">الإعلانات</span>
                </button>
            </div>
    </div>
    </RadzenFooter>
</RadzenLayout>


